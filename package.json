{
  "name": "daily-call-reporting-system",
  "version": "1.0.0",
  "description": "Daily Call Reporting System for Pharmaceutical Sales Representatives",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install",
    "start": "netlify dev"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
